// Production config. Copy to config.js and set DATA_BASE to your R2 public URL.
// config.js is gitignored and only used in deployed builds — local dev falls back to "data".
window.DATA_BASE = "https://pub-XXXXXXXXXXXX.r2.dev";
