pipeline/raw/
pipeline/derived/
web/data/
web/config.js
.cloudflare-deploy.env
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
.DS_Store
.claude/
.wrangler/
# Bare (not web/-prefixed): vitest also drops a .vite cache in a node_modules/
# at the repo root when it is invoked from outside web/.
node_modules/
web/coverage/
pipeline/htmlcov/
.coverage
# vitest config HMR leftover artifacts
web/vitest.config.js.timestamp-*.mjs

# Playwright MCP screenshot scratch (recreated every browser session)
.playwright-mcp/
